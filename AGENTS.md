# void

Minimalist Kotlin Compose Android launcher. Follow `CLAUDE.md`. `src/main` is the only runtime source set. ApplicationId: `com.voidlauncher.app`.

Prefer `kotlin-patterns`, `material-design`, `android-clean-architecture`. Use `FeatureAvailability` for capability gates. Review with `ecc-kotlin-reviewer`.

Verify: `./gradlew assembleDebug testDebugUnitTest lintDebug`
